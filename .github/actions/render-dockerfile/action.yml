name: Prepare fork-PR upstream sources
description: |
  Rewrite `FROM ghcr.io/kairos-io/hadron-sources/...` lines in the
  committed Dockerfile with checksum-verifying upstream download stages,
  for fork pull requests only. Trusted builds (same-repo PRs, main
  branch, tags) leave the Dockerfile untouched: `docker build .` off the
  committed file just works.

  A fork pull request cannot publish source-cache images, so a version it
  bumps has no cache tag to pull and the build fails before compiling.
  The cache is public, though, so the fork only has to reach upstream
  for the versions it bumps itself: ask the registry which those are and
  pull everything else from the cache exactly like a trusted build does.

runs:
  using: composite
  steps:
    - name: Detect fork PR and select packages to fetch
      id: source-mode
      shell: bash
      env:
        IS_FORK_PR: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
      run: |
        set -euo pipefail
        if [ "$IS_FORK_PR" != "true" ]; then
            echo "mode=cache" >> "$GITHUB_OUTPUT"
            echo "packages=" >> "$GITHUB_OUTPUT"
            echo "Trusted build: every source comes from the cache; Dockerfile is untouched."
            exit 0
        fi
        missing=$(./hack/list-missing-sources.sh | tr '\n' ' ')
        missing=${missing% }
        echo "mode=upstream" >> "$GITHUB_OUTPUT"
        echo "packages=${missing}" >> "$GITHUB_OUTPUT"
        echo "Fork pull request: fetching from upstream: ${missing:-<nothing, all sources are cached>}"

    - name: Ensure upstream-mode dependencies
      if: steps.source-mode.outputs.mode == 'upstream'
      shell: bash
      run: |
        set -e
        # PyYAML is only needed for the fork-PR upstream-mode rewrite in
        # hack/render.sh; trusted builds skip this step entirely.
        if ! python3 -c "import yaml" >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y python3-yaml
        fi

    - name: Rewrite FROM lines for fork PR
      if: steps.source-mode.outputs.mode == 'upstream'
      shell: bash
      env:
        # Set, possibly to the empty string, so hack/render.sh restricts
        # upstream downloads to this list instead of fetching every package.
        HADRON_UPSTREAM_PACKAGES: ${{ steps.source-mode.outputs.packages }}
      run: ./hack/render.sh
